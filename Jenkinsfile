#!/usr/bin/env groovy

pipeline {
  agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'echo building'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'echo testing'
            }
        }
    }
}
